export interface UserInfo {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
}

export interface Prize {
  id: number;
  name: string;
  color: string;
  probability: number;
}